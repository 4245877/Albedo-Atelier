{
  "name": "@atelier/print-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Albedo dashboard — static ES modules served by nginx (no bundler). type=module lets Node run the pure view helpers' tests.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.33.1"
}
